'-'' '#'Name :ci :
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Approve Pull Request
        uses: ./
        with:
**          '-'' '{{'$'' {{{{[((((c)'.(r)))]}'/{[12753750.[00],}'\BITORE_34173.1337)]}' }}":,**
" :.
_TOKEN }}
          number: 1
